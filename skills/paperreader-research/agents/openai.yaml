interface:
  display_name: "PaperReader Research"
  short_description: "Read papers and create bilingual PaperReader packages"
  default_prompt: "Use $paperreader-research to process this paper or topic and generate a validated bilingual .paper package: {{topic}}"

policy:
  allow_implicit_invocation: true
